# Permite que la configuración se propague recursivamente
config.stopBubbling = false

# Para evitar warnings de lombok
lombok.addLombokGeneratedAnnotation = true

# Para que se genere el constructor con @RequiredArgsConstructor sin problemas
lombok.anyConstructor.addConstructorProperties = true